package main

func main() {
	// mongoClient, _ := mongodb.NewMongoDB(mongodb.MongoDBConfig{
	// 	Uri:    "uri",
	// 	DbName: "dbname",
	// })

	// minioClient, _ := minio.NewMinIOClient("1", "2", "3")

	// imageRepo := repository.NewImageMongoDBRepository(mongoClient)
	// imageFS := repository.NewMinIOImageStorage(minioClient)

	// imageService := service.NewImageService(imageRepo, imageFS)
	// imageUsecase := usecase.NewImageUsecase(imageService)
}
